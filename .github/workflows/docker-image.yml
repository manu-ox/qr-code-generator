name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  build_and_push_docker_image:
    
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Build the Docker image
      run: docker build -t manukrishnaos/qr-code-generator .

    - name: Push Docker image
      run: docker push manukrishnaos/qr-code-generator

    
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build_and_push_docker_image

    steps:

    - name: remote ssh command
      uses: appleboy/ssh-action@v1.2.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: mk
        password: ${{ secrets.SERVER_PASSWORD }}
        port: 22
        script: whoami


        # script: /bin/sh scripts/deploy-qr.sh
          # docker pull manukrishnaos/qr-code-generator
          # docker stop ${{ secrets.DOCKER_CONTAINER_NAME }}
          # docker rm ${{ secrets.DOCKER_CONTAINER_NAME }}
          # docker run -dt -p ${{ secrets.PORT }}:8000 --name ${{ secrets.DOCKER_CONTAINER_NAME }} manukrishnaos/qr-code-generator
